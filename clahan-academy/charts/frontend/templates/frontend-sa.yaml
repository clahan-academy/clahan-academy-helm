# helm/clahan-academy/templates/serviceaccounts/frontend-sa.yaml

apiVersion: v1
kind: ServiceAccount
metadata:
  name: clahan-frontend-sa
  namespace: {{ .Values.global.namespace }}
  annotations:
    azure.workload.identity/client-id: {{ .Values.global.azure.workloadIdentity.frontend.clientId | quote }}
  labels:
    azure.workload.identity/use: "true"
