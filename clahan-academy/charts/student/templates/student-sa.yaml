# helm/clahan-academy/templates/serviceaccounts/student-sa.yaml

apiVersion: v1
kind: ServiceAccount
metadata:
  name: clahan-student-sa
  namespace: {{ .Values.global.namespace }}
  annotations:
    azure.workload.identity/client-id: {{ .Values.global.azure.workloadIdentity.student.clientId | quote }}
  labels:
    azure.workload.identity/use: "true"
