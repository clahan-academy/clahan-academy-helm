# helm/clahan-academy/templates/deployments/student-deployment.yaml

apiVersion: apps/v1
kind: Deployment
metadata:
  name: student-service
  namespace: {{ .Values.global.namespace }}
  labels:
    app: student-service
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: student-service
  template:
    metadata:
      labels:
        app: student-service
        azure.workload.identity/use: "true"
    spec:
      serviceAccountName: clahan-student-sa
      containers:
        - name: student
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          env:
            - name: KEY_VAULT_NAME
              value: {{ .Values.global.azure.keyVaultName | quote }}
          ports:
            - containerPort: {{ .Values.port }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop: ["ALL"]
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.port }}
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.port }}
            initialDelaySeconds: 10
            periodSeconds: 5
            failureThreshold: 3
