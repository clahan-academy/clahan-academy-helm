# helm/clahan-academy/templates/services/exam-svc.yaml

apiVersion: v1
kind: Service
metadata:
  name: exam-service
  namespace: clahan-academy
  labels:
    {{- include "clahan.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.port }}
      targetPort: {{ .Values.port }}
      protocol: TCP
      name: http
  selector:
    app: exam-service
