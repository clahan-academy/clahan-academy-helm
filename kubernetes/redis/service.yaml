# FOR DEMO/EVALUATION ONLY
# Production uses Azure Cache for Redis Standard C1
apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: production
  labels:
    app: redis
spec:
  type: ClusterIP
  selector:
    app: redis
  ports:
    - port: 6379
      targetPort: 6379